interface:
  display_name: "转念 / Shift Mind"
  short_description: "给一句话换一种看世界、看生活、看自己的方式。"
  default_prompt: "Use $shift-mind to rewrite this sentence through one named filter and output 3 short final versions."

policy:
  allow_implicit_invocation: true
